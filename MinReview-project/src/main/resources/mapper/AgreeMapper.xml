<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="peng.zhi.liu.mapper.AgreeMapper">

    <!-- 添加点赞记录 -->
    <insert id="addAgreeMapper" parameterType="peng.zhi.liu.entity.Agree">
        insert into agree (
            dish_id,
            user_id,
            create_time,
            update_time
        ) values (
            #{dishId},
            #{userId},
            #{createTime},
            #{updateTime}
        )
    </insert>

    <!-- 删除点赞记录 -->
    <delete id="deleteAgreeMapper">
        delete from agree
        where dish_id = #{dishId}
        and user_id = #{userId}
    </delete>

</mapper>