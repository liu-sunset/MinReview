<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="peng.zhi.liu.mapper.DisagreeMapper">

    <!-- 添加点踩记录 -->
    <insert id="addDisagreeMapper" parameterType="peng.zhi.liu.entity.Disagree">
        insert into disagree (
            dish_id,
            user_id,
            create_time,
            update_time
        ) values (
            #{dishId},
            #{userId},
            #{createTime},
            #{updateTime}
        )
    </insert>

    <!-- 删除点踩记录 -->
    <delete id="deleteDisagreeMapper">
        delete from disagree
        where dish_id = #{dishId}
        and user_id = #{userId}
    </delete>

</mapper>